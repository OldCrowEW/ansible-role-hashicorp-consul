---
# handlers file for ansible-role-hashicorp-consul
- name: restart-consul
  systemd:
    name: consul
    state: restarted
    daemon_reload: true
  when: consul_systemd_enabled

- name: remove-consul
  include_tasks: remove-consul.yml
